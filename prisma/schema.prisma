generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Categories {
  id Int @id @default(autoincrement())
}

model Description {
  id          Int      @id @default(autoincrement())
  title       String?
  userName    String?
  country     String?
  createdAt   DateTime @default(now()) @db.DateTime(0)
  imageURL    String?
  fish        String?
  phoneNumber String?  @unique(length: 11)
  tag         String?
  desc        String?  @db.LongText
  uploadDate  String?
}

model User {
  id      Int    @id @default(autoincrement())
  desc    String
  country String
  fishs   String
}
